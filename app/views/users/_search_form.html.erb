<%= form_with url: search_path(user), method: :get, local: false do |f| %>
  <%= f.date_field :created_at %><!--年月日が表示される-->
    <%= f.submit '検索', class: 'btn btn-primary' %>
<% end %>
<div class="search_form_table"></div>
